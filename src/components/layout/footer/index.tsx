import React from 'react';
import Box from '@mui/material/Box';

interface Props {}

const Footer = (props: Props) => {
    return <Box component="div" mt={5} sx={{ textAlign: 'center' }}>STFU and click to be on top!</Box>
}

export default Footer;
